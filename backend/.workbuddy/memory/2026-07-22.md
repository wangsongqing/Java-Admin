# 2026-07-22 工作日志

## Java-Admin 项目目录结构审查

对 `backend/` 下的 Spring Boot 项目做了目录结构评估。

**技术栈**：Spring Boot 3.2.5 + Java 17 + MyBatis-Plus 3.5.5 + Sa-Token 1.39.0 + Flyway 10.11.0 + Hutool + Lombok。是一个 RBAC 权限管理后台（User/Role/Permission + UserRole/RolePermission 五表）。

**结论**：标准三层架构（controller→service→mapper），骨架规范，无硬伤。

**已做对的点**：
- service 接口与 impl 分离；common/Result 统一响应；exception 全局异常处理
- User 实体的 password/deleted 已 @JsonIgnore
- Flyway 脚本放 db/migration，版本号 V1~V4；Mapper XML 放 resources/mapper
- 启动类在根包 com.admin

**待改进项（按优先级）**：
1. dto 包混入 LoginVO（出参）→ 建议拆出 vo 包
2. config 包职责杂：含配置类 + PermissionProperties + PermissionSyncRunner(应归 runner/bootstrap) + StpInterfaceImpl(应归 security)
3. 缺 src/test 目录（已引 starter-test 但无测试）
4. 缺 enums/constants 包，权限码字符串、status 魔法值散落

用户尚未确认是否动手改，等待回复。

## 目录结构调整（已执行）

用户确认后，完成以下 4 项重构，`mvn test-compile` 与单测均通过：

1. **拆出 vo 包**：`LoginVO` 从 `dto/` 移到新建 `vo/` 包；更新 AuthController、UserService、UserServiceImpl 的 import（把 `dto.*` 通配改为精确 import）。
2. **理清 config 包**：
   - `PermissionSyncRunner` → 新建 `runner/` 包（需 import config.PermissionProperties）
   - `StpInterfaceImpl` → 新建 `security/` 包
   - config 现仅剩 FlywayConfig、MybatisPlusConfig、PermissionProperties
3. **补 enums/constants 包**：
   - 新建 `constant/PermissionCode`（权限码常量）、`constant/RoleCode`（角色码常量），替换三个 controller 里 @SaCheckPermission/@SaCheckRole 的字符串字面量
   - 新建 `enums/StatusEnum`（DISABLED=0/NORMAL=1，含 isNormal/isDisabled 静态方法），替换 UserServiceImpl 的 `getStatus()==0` 和 PermissionSyncRunner 的默认值 `1`
4. **补 src/test**：新建 `test/ResultTest`、`test/StatusEnumTest` 两个纯单元测试（不依赖 Spring 上下文/数据库，可独立跑通）

新增包：vo/、runner/、security/、constant/、enums/、src/test/。
注意：@SaCheckPermission 注解引用常量必须是 `public static final String` 编译期常量。

